[all]
music = True
vsync = True

# Try force_audio if you're having sound troubles. It forces a
# particular audio subsystem to be used.
#
# Some possible values are:
#    - alsa (Linux only)
#    - directsound (default on Windows, doesn't work anywhere else)
#    - openal (default on Linux)
#    - silent (a good fallback if audio is causing game crashes)
# force_audio = alsa

