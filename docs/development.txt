
Sinister Ducks has the following dependencies you'll need to run the game
from source:

    Python 2.6
    Python Package pyglet

Binaries for libavbin are included in the tree, so you don't need to install
that.

To run the tests, you'll need:

    Python Package mock
    Python Package unittestplus
    Python Package nose (for test discovery)

